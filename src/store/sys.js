// import apis from '@/apis'
// import http from '@/tools/http'
// import _ from '@/tools/lib/lodash'

const state = {
  // // 所有商户
  // offices: []
}

const mutations = {
  // updateOffices (state, data) {
  //   state.offices = data
  // }
}

const actions = {
  // getAllOffice ({ commit }) {
  //   http.get({
  //     url: apis.office.getAllOffice,
  //     loading: false,
  //     success: ({ data }) => {
  //       commit('updateOffices', data)
  //     }
  //   })
  // }
}

const getters = {
  // getOffices: state => state.offices
}

export default {
  actions,
  getters,
  state,
  mutations
}
